<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="MauiCollectionNavSample.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:MauiCollectionNavSample.ViewModels">

    <ContentPage.BindingContext>
        <viewmodels:MainViewModel />
    </ContentPage.BindingContext>

    <CollectionView
        x:Name="CollectionViewRef"
        ItemsSource="{Binding Items}"
        SelectionMode="Single"
        SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
        <CollectionView.GestureRecognizers>
            <TapGestureRecognizer
                Command="{Binding CollectionTappedCommand}"
                CommandParameter="{Binding SelectedItem, Source={x:Reference CollectionViewRef}}" />
        </CollectionView.GestureRecognizers>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Border
                    Stroke="LightGray"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 10"
                    Padding="10"
                    Margin="5">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="LightBlue" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <Label Text="{Binding Title}" FontSize="18"/>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
